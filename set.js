var novoSet = new Set();

novoSet.add('backend');
novoSet.add('frontend');
novoSet.add('mobile');

//Set basicamente não deixa fazer duplicatas, EX : tags
var resultado = novoSet;

console.log(resultado)
